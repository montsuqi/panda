test1	{
	count	int;
	code	int;
};

primary	{
	count, code;
};

path	dbw	{
	DBSELECT	{
		SELECT	*
			FROM test1
			WHERE
				count <= :count
			LIMIT
				: $limit
		;
	};
};

readonly	path	dbr	{
	DBSELECT	{
		SELECT	*
			FROM test1
			WHERE
				count <= :count
			LIMIT
				: $limit
		;
	};
};

