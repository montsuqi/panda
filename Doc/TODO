TODOとアイディア

□エラー

	glclientがwidgetのデータの揃いが悪いと異常終了するらしい(PandaCList等)

	apsのキャッシュの制御がおかしい(expireすべき時にしなかったり、初期化異常がある)

□テスト中

	LDまたぎのポップアップ

	gladeファイルの修正について
    	recファイルで宣言しているウィジェットを削除すると画面が表示されなくなる。

	headでポインタの踏み外しがあるようだ

□ラージオブジェクトの実装について

	  ラージオブジェクトは、panda内ではMonObjectで扱うが、PLserver内で
	binaryに変換する。将来的にはPLclientでのオンデマンド転送も可能にす
	るが、Ver 1.x系では実現しない。

□データベースハンドラ追加

	full text search
	native XML database

□libmonpic内の処理の改良(未実装)

	  現在、peerの生死判定は上位プロトコルでPING-PONGすることによって
	行っている。これをlibmonpicの中で行うようにする。

□改良

	UNIX domain socketの消去
	ValueStructの改良(NameTree)
	メッセージログを整備する
	mcp.dc.puttypeを数値にする
	.dbでfunctionとoperationの区別をつける。functionの時にはリダイレクトしない

□拡張

	DBを実効ユーザ毎に開けるようにする
	障害時の自動復旧
	多データベースエンジン対応
	データ交換形式の抽象化
	分散データベース対応
	トランザクション管理の改良
	トランザクションログの取り方の改良(現状だとcommit中のログ取りに問題がある)
	ログをソースレベルで取得する
	dbredirectorにファイルからの入力機能を持たせる
	単体テスト環境の作成

□作業中

	webインターフェイス(QuestionLaborで対応中)
	多プログラム言語対応
	ロードバランス(まだ改良の余地あり)
	glade -> HTC変換の改良
	BLOBをもっとマシなものにする

□完了

	glade, libgladeのfork -> MONSHA
	termnameの長さを見直す(現状36byte)[middle:04214]
		現状では、複数のプレゼンテーションサーバを使うと、重複する危険性がある
		64byteに決定(空いたら適当にリザーブにする)
	トラフィックの暗号化
	認証局対応
	libの整理
	glserverのプロセスが残る問題[middle:3484]
	通信の最適化
	aps端末キャッシュ
	複数画面のPUT
	よりロバストにする(画面名を間違えるとwfcごと落ちる)
	国際化
	Cハンドラ(拡張中)
	Rubyハンドラ(前田君が拡張中)
	DBRuby
	dbcodeのサイズの制限をなくす(前田君がやった)
	binaryのハンドリング
	net.cの効率化(できる限りバッファリングする)
	Webクライアントのスペースの表示について
		プログラムでスペースを表示するよう設定するが表示されない。
	Webクライアントのnumberの表示について
		numberを表示すると最後にゴミがついてくる
	いわゆるラージデータの処理
	htcgen2で自動作成されるCListについて
		WebクライアントでCListの部分が1列しか表示されない。

□放置

	インチキ証明書対応
	必要度の低いthreadを減らす
	termthreadを非スレッド化する

	ipv4アドレスのエンコードが同一になってしまう[middle:04214]
		--with-ipv6の時にIPv4で接続すると、アドレスが同じになる(本来はlibcが射影アドレスを間違えているバグ)

